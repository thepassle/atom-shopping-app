{
  "scripts": {
    "test": "web-test-runner --coverage",
    "test:watch": "web-test-runner --watch",
    "start": "web-dev-server --app-index index.html --node-resolve --open --watch",
    "build": "rimraf dist && rollup -c rollup.config.js",
    "start:build": "npm run build && web-dev-server --root-dir dist --app-index index.html --open"
  },
  "devDependencies": {
    "@open-wc/building-rollup": "^1.9.4",
    "@open-wc/testing": "^2.0.0",
    "@vaadin/router": "^1.7.2",
    "@web/dev-server": "^0.0.12",
    "@web/test-runner": "^0.7.41",
    "deepmerge": "^4.2.2",
    "rimraf": "^3.0.2",
    "rollup-plugin-copy": "^3.3.0"
  },
  "name": "shopping-app",
  "version": "0.0.0",
  "description": "Webcomponent shopping-app following open-wc recommendations",
  "author": "shopping-app",
  "license": "MIT",
  "dependencies": {
    "@klaxon/atom": "0.0.6",
    "lit-element": "^2.0.1",
    "lit-html": "^1.0.0"
  }
}
